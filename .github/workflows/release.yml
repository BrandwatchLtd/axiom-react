  
name: Run tests
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  build_packages:
    name: 'Build packages'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '12.x'
    - uses: actions/cache@v1
      env:
          cache-name: cache-node-modules
      with:
        path: ~/.npm
        key: ${{ runner.sha }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
    - run: yarn install --frozen-lockfile
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  test:
    needs: build_packages
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v1
      env:
          cache-name: cache-node-modules
      with:
        path: ~/.npm
        key: ${{ runner.sha }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
    - run: yarn install --frozen-lockfile
    - run: yarn test
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}